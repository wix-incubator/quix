version: '3'
services:
  backend:
    build: ./quix-backend/
    ports:
      - "8081:8081"
    depends_on:
      - presto
    environment:
      - MODULES
      - PRESTO_API
      - ATHENA_OUTPUT
      - ATHENA_REGION
      - ATHENA_DATABASE
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_KEY
      - AUTH_COOKIE
      - AUTH_SECRET
      - AUTH_TYPE
      - PRESTO_DB_EMPTY_TIMEOUT
      - PRESTO_DB_REQUEST_TIMEOUT
  frontend:
    build: ./quix-frontend/
    ports:
      - "3000:3000"
    depends_on:
      - backend
      - db
    environment:
      - DB_NAME
      - DB_USER
      - DB_PASS
      - DB_HOST
      - DB_PORT
      - BACKEND_INTERNAL_URL
      - BACKEND_PUBLIC_URL
      - GOOGLE_SSO_CLIENT_ID
      - GOOGLE_SSO_SECRET
      - AUTH_COOKIE
      - AUTH_SECRET
      - AUTH_TYPE
      - APPMETRICS_PORT
      - ENABLE_APPMETRICS
      - MODULES
  db:
    image: "mysql:5.7"
    restart: always
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: "quix"
    volumes:
      - "db_volume:/var/lib/mysql"
  presto:
    image: "starburstdata/presto"
    ports:
      - "8181:8080"

volumes:
  db_volume:
