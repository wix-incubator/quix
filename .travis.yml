matrix:
  include:
    - stage: test
      name: "Client"
      language: node_js
      node_js:
        - "11"
      cache: npm
      install:
        - cd ./quix-frontend/client
        - npm install
      script:
        - cd ./quix-frontend/client
        - npm test
    - stage: test
      name: "Frontend"
      language: node_js
      node_js:
        - "11"
      services:
        - mysql
      cache: npm
      before_install:
        - mysql -e 'CREATE DATABASE IF NOT EXISTS Quix;'
      install:
        - cd ./quix-frontend/service
        - npm install
      addons:
        apt:
          sources:
            - mysql-5.7-trusty
          packages:
            - mysql-server
            - mysql-client
      script:
        - cd ./quix-frontend/service
        - npm test:mysql
    - state: test
      name: "Backend"
      language: java
      cache:
        directories:
          - $HOME/.m2
      install:
        - cd ./quix-backend
        - mvn install
      script:
        - cd ./quix-backend
        - mvn test